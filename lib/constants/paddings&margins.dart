import 'package:flutter/material.dart';

class Paddings {
  static const EdgeInsetsGeometry appBarIconPadding =
      EdgeInsets.symmetric(horizontal: 12);

  static const EdgeInsetsGeometry popUpMenuItemPadding =
      EdgeInsets.symmetric(horizontal: 10, vertical: 10);

  static const EdgeInsetsGeometry sendButtonPadding = EdgeInsets.all(10);

  static const EdgeInsetsGeometry prefixIconPaddig =
      EdgeInsets.only(left: 20.0, right: 10.0);

  static const EdgeInsetsGeometry chatTextFieldContentPadding =
      EdgeInsets.symmetric(horizontal: 5, vertical: 14);
}

class Margins {
  static const EdgeInsetsGeometry appBarIconMargin = EdgeInsets.only(right: 20);
}
